const user:(string | number)[]=['ten',1]
//here in normal array the order doesnt matter
// we can write either 1 first then ten

//but in tuple the structure should be same
//consider tuple as a restricted array

let tuuser:[string,number,boolean] //remember here the types are written in the square bracket


tuuser=["tenzin",12,true]

// tuuser=[12,"tenzin","delek"] //result in fault as the order is wrong

let rgb:[number,number,number]=[255,122,233]

type User =[number,string]

const user1:User =[123, "tenzin"]
